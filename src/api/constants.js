//const currentUrl = `${window.location.protocol}//${window.location.hostname}:${window.location.port}`;

//export const serverAPIVersion = 1;
export const urlHost = process.env.REACT_APP_SERVER_URL;

